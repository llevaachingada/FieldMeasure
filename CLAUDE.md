# CLAUDE.md — Field Measure

**This file is deliberately a pointer. Do not maintain a second copy of the instructions here.**

`AGENTS.md` is canonical for agent instructions: reading order, the authority chain, the
non-negotiables, the working rules, `[Surface]` gate policy, and when to stop and ask the human.

**State** — what has actually shipped and where the previous session stopped — lives in
`docs/CONTINUITY.md` and the last entry of `docs/BUILD-LOG.md`. Never infer progress from an
instruction file; the two copies that used to live here drifted for three sessions and still claimed
no code existed after five slices had shipped.
