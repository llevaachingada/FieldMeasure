/**
 * Slice 0.1 placeholder screen. Nothing domain-specific exists yet, so no
 * user-visible copy has been added to `src/ui/strings.ts` — the string table is
 * empty by spec (`export const STRINGS = {} as const`). The two literals below
 * are scaffold-only and move to `docs/appendix-strings.md` / `STRINGS` when the
 * first real screen (slice 0.3) lands.
 */
export default function App() {
  return (
    <main className="scaffold">
      <h1>Field Measure</h1>
      <p>Scaffold — no app features yet.</p>
    </main>
  );
}
